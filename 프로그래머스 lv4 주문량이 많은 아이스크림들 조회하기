SELECT FLAVOR
FROM( SELECT A.FLAVOR, SUM(A.TOTAL_ORDER)+SUM(B.TOTAL_ORDER) AS TOTAL_SUM
      FROM FIRST_HALF AS A
      INNER JOIN JULY AS B
      ON A.FLAVOR = B.FLAVOR
      GROUP BY FLAVOR
      ORDER BY TOTAL_SUM DESC
    ) AS TOTAL_TABLE
LIMIT 3
